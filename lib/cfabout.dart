import 'package:flutter/material.dart';

class Cfabout extends StatelessWidget {
  const Cfabout({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(




      body: Column(
        children: [
          Container(
            decoration: BoxDecoration(
              color: Colors.amber
            ),
          )
        ],
      ),
    );
  }
}